services:
  agent:
    build:
      context: agent
      dockerfile: Dockerfile
    env_file:
      - .env
    stdin_open: true
    tty: true
  sandbox:
    build:
      context: sandbox
      dockerfile: Dockerfile
